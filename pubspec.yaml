name: focus_on_it
description: Get notified when a widget is focused or unfocused.
version: 1.0.0
repository: https://github.com/luanemanuel/focus_on_it/tree/master
issue_tracker: https://github.com/luanemanuel/focus_on_it/issues

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  visibility_detector: ^0.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  very_good_analysis: ^3.1.0

flutter:
